# 001-1 技術選定 ADR

## ADR-0001: サービス分割方針

**Status**: Accepted  
**Date**: 2026-06-03

### Context
Canva / Google Slides スケールへの成長を見越し、初日からモノリスにしない。

### Decision
4つの独立した Go サービス + Next.js + Nginx の構成を採用。

| サービス | 責務 | スケール戦略 |
|---------|------|------------|
| api | CRUD, Auth | 水平スケール（ステートレス） |
| collab | Yjs WebSocket | 水平スケール（Sticky Session or Redis Pub/Sub） |
| realtime | Presenter配信 | 水平スケール（Redis Pub/Sub） |
| assets | ファイルI/O | 垂直スケール or CDN オフロード |

将来 Redis Pub/Sub を追加すれば collab/realtime は完全水平スケール可能。

---

## ADR-0002: バックエンドアーキテクチャ（Hexagonal Architecture）

**Status**: Accepted  
**Date**: 2026-06-03

### Decision
`services/api` は Hexagonal Architecture（六角形アーキテクチャ）で実装。

```
domain → application → infrastructure
（外部依存ゼロ）（DBはインターフェース経由）（実装詳細）
```

### Consequences
- ドメインロジックが HTTP / DB に汚染されない
- テストでリポジトリをモックするだけで済む
- DB を PostgreSQL から別 DB に変えても domain/application 無変更
- サービス分割時に domain + application をそのまま新サービスに移植可能

---

## ADR-0003: フロントエンドアーキテクチャ（Feature Slices）

**Status**: Accepted  
**Date**: 2026-06-03

### Decision
`web/src/features/` 配下は縦断スライス（Vertical Slice / Feature-based）構造。

各 feature は独立したコンポーネント・ストア・フックを持つ。
feature 間の直接 import を禁止し、共有が必要なものは `shared/` に昇格。

### Consequences
- 機能追加が他機能に影響しない
- 将来マイクロフロントエンドに分割可能
- テストが feature 単位で完結する

---

## ADR-0004: キャンバスエンジン（Fabric.js v6）

**Status**: Accepted  
**Date**: 2026-06-03

### Decision
Fabric.js v6 を採用。

| 比較 | Fabric.js v6 | tldraw | カスタムSVG |
|------|-------------|--------|------------|
| 成熟度 | 高 | 中 | — |
| スライドエディタ適性 | 高 | 中（whiteboard向き）| 高 |
| SVGエクスポート | ✓ | ✓ | ✓ |
| Bezier編集 | 拡張で可 | ✓ | フルコントロール |
| 開発速度 | 速 | 速 | 遅 |

Phase 4 のベジェ/ノード編集は `fabric.Path` に custom overlay で拡張。

---

## ADR-0005: リアルタイム通信戦略

**Status**: Accepted  
**Date**: 2026-06-03

### Decision
用途別にプロトコルを分ける:

| 用途 | プロトコル | サービス |
|------|-----------|---------|
| 共同編集 | Yjs y-websocket | collab |
| プレゼン配信 | カスタム WebSocket | realtime |
| API | REST | api |

Yjs は CRDT でコンフリクトフリー。Notion, Linear, Figma の共同編集基盤と同等。

---

## ADR-0006: 状態管理（Zustand + Immer）

**Status**: Accepted  
**Date**: 2026-06-03

### Decision
Zustand + Immer を採用。

- Redux: ボイラープレート過多
- Jotai: アトミック型で大規模状態管理に不向き
- Zustand: シンプル、TypeScript 相性良好、Immer で immutable 操作が簡単

スライドデータは Zustand ストアが保持し、Yjs doc と双方向同期する。

---

## ADR-0007: AI 統合（完全ローカル）

**Status**: Accepted  
**Date**: 2026-06-03

### Decision
全 AI はローカル実行。外部 API コスト ゼロ。

| 用途 | モデル | エンドポイント | ライブラリ |
|------|--------|--------------|----------|
| テキスト生成 | LFM2-2.6B | localhost:4242 | openai (npm) OpenAI互換 |
| 音声文字起こし | Whisper.cpp | localhost:8765 | カスタムHTTPクライアント |
| 背景除去（Phase 2） | rembg | localhost:8766 | カスタムHTTPクライアント |

LFM Gateway は OpenAI 互換 API なので、`openai` npm パッケージを BASE_URL 変更だけで使用可能。

---

## ADR-0008: エクスポート戦略

**Status**: Accepted  
**Date**: 2026-06-03

| 形式 | 実装方法 |
|------|---------|
| PDF | jsPDF + html2canvas（クライアントサイド） |
| PNG/JPEG | Fabric.js `canvas.toDataURL()` |
| SVG | Fabric.js `canvas.toSVG()` |
| PPTX | pptxgenjs（クライアントサイド） |
| 動画 | MediaRecorder API + canvas.captureStream() |

---

## ADR-0009: 認証（JWT 2トークン方式）

**Status**: Accepted  
**Date**: 2026-06-03

### Decision
アクセストークン(15分) + リフレッシュトークン(7日)の2トークン方式。

- アクセストークン: Authorization: Bearer ヘッダー
- リフレッシュトークン: HttpOnly Cookie（XSS対策）
- リフレッシュ時に新しいペアを発行（ローテーション）

---

## ADR-0010: DBスキーマ設計方針

**Status**: Accepted  
**Date**: 2026-06-03

### Decision
スライドコンテンツは Fabric.js JSON を JSONB 型で保存。

```sql
CREATE TABLE slides (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  presentation_id UUID NOT NULL REFERENCES presentations(id),
  position    INTEGER NOT NULL,
  thumbnail   TEXT,
  content     JSONB NOT NULL DEFAULT '{}',  -- Fabric.js JSON
  created_at  TIMESTAMPTZ DEFAULT NOW(),
  updated_at  TIMESTAMPTZ DEFAULT NOW()
);
```

メリット: スキーマ変更不要でFabric.jsのあらゆるオブジェクトを格納可能。
PostgreSQL の JSONB はインデックスも貼れるため検索も可能。

主要テーブル:
- users
- presentations
- slides (content: JSONB)
- assets
- comments
- presentation_members (コラボレーター権限)
- refresh_tokens

---

## ADR-0011: 協調編集のCRDTライブラリ選定（Yjs採用）

**Status**: Accepted
**Date**: 2026-06-08
**補強対象**: ADR-0005（リアルタイム通信戦略）— 既に「共同編集 = Yjs y-websocket」と決めていた選定を、性能比較の実測を踏まえて正式な ADR として確定・補強する。

### Context
複数ユーザーが同一プレゼンを同時編集する共同編集機能を実装する。
オフライン編集・低レイテンシ・コンフリクトフリーなマージが要件。
ADR-0005 で方向性として Yjs を挙げていたが、CRDT 実装の二大候補（Yjs / Automerge）を
実測ベンチで比較し、ライブラリを正式に確定する必要があった。

データモデルは二層構成とする:

- **Yjs doc = 真実源（source of truth）**: ライブ編集中の状態は Yjs CRDT が保持。
- **`slides.content`（JSONB）= materialized 投影**: 表示・エクスポート・既存 `UpdateContent` 用の射影。
  Yjs doc から定期的に materialize する。これにより既存の JSONB ベース永続化（ADR-0010）と両立する。

### Decision
**CRDT ライブラリに Yjs（`yjs` + `y-websocket`）を採用する。**

- スライド列 = `Y.Array<Y.Map>`、各スライドが `Y.Map`、スライド内オブジェクトは `Y.Array<Y.Map>`。
- 接続は presentation 単位の「room」を `y-websocket` で確立。再接続は y-websocket が内部で指数バックオフ処理。
- フロントは Yjs doc を保持し、materialized な `SlideContent` へ射影するヘルパを持つ
  （`web/src/lib/collab/schema.ts`）。

### Alternatives Considered

| 観点 | Yjs | Automerge |
|------|-----|-----------|
| 更新適用スループット（1万ops） | 速い（最適化された binary encoding） | 遅い（約3〜10倍のオーバーヘッド） |
| ドキュメントサイズ（同一編集列） | 小（差分エンコード効率が高い） | 大（履歴メタデータが重い） |
| メモリ使用量 | 低 | 高（全変更履歴を保持しがち） |
| WebSocket 連携 | `y-websocket` が公式・実績多数 | 自前実装寄り |
| エコシステム / 実績 | Notion・Linear 等、共同編集基盤で広く採用 | 採用例はあるが Yjs より少 |
| 型付き共有型（Y.Map/Y.Array） | リッチで Fabric オブジェクトとの親和性高 | JSON 風で柔軟だが性能トレードオフ |

実測ベンチ（編集列の適用時間・ドキュメントサイズ・メモリ）で Yjs が一貫して優位だったため Yjs を採用。

### Consequences
- **Pro**: 低レイテンシ・小さい同期ペイロード。`y-websocket` で接続層の実装コストが小さい。
  既存 JSONB 永続化を materialized 投影として温存でき、ADR-0010 と非破壊で両立。
- **Con**: Yjs 特有のデータモデル（共有型は doc にアタッチしないと読めない等）に実装側が習熟する必要がある。
  真実源（Yjs doc）と投影（JSONB）の二層を一貫させる materialize ロジックが追加で必要。
- **段階導入**: 本 PR では接続層と doc スキーマのみ確立する。Fabric との双方向同期は別 PR、
  awareness（カーソル共有等）はさらに後続 PR で実装する。
